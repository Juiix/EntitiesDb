<#@ template language="C#" #>
<#@ output extension=".cs" #>
<#@ import namespace="System.Text" #>
<#@ include file="Generics.ttinclude" #>
namespace EntitiesDb;

public partial struct Chunk
{
<#
	for (int index = 2; index < GenericCount; index++)
	{
		var generics = Generics(index);
		var checks = FormatSep(index, "Has(ids.T{0})", " &&" + Environment.NewLine);
#>
	/// <inheritdoc cref="Has{T0}(in ComponentIds{T0})"/>
	public readonly bool Has<<#= generics #>>(in ComponentIds<<#= generics #>> ids) =>
		<#= Indent(checks, 8) #>
		;
<#
	}
#>
}